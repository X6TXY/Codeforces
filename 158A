#include <bits/stdc++.h>
 
using namespace std;
 
int main(){
    int n,m,ps=0;
    cin >> n >> m;
    int a[n];
    for(int i=0;i<n;i++){
        cin>>a[i];
    }
    for(int i=0;i<n;i++){
        if(a[i]>=a[m-1] and a[i]!=0 ) ps++;
    }
    cout <<ps;
}
